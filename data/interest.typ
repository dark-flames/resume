#import "../libs.typ": *

#let interest(env) = {
  
  cv-content(env, {
    multiLang(env, en: [== Research Interests])
    [
      - Implemention of dependently typed programming languages, focusing on elaboration and pattern matching.
      - Semantic models and methods of dependent type theories, including categorical semantics, normalization-by-evaluation, and logical relations.
      - Integration of dependent types with other type systems, such as gradual typing and effect systems.
    ]
  })
}
